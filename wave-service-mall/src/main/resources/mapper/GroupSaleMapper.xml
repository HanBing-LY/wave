<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.langchao.waveservicemall.mapper.GroupSaleMapper">
    <resultMap id="BaseResultMap" type="com.langchao.waveservicemall.pojo.GroupSale">
            <id column="id" jdbcType="INTEGER" property="id"/>
            <result column="flag" jdbcType="INTEGER" property="flag"/>
            <result column="start_time" jdbcType="TIMESTAMP" property="startTime"/>
            <result column="end_time" jdbcType="TIMESTAMP" property="endTime"/>
    </resultMap>

    <sql id="baseSql">group_sale.id id,group_sale.flag flag,group_sale.start_time startTime,group_sale.end_time endTime</sql>

    <select id="list" resultType="com.langchao.waveservicemall.pojo.GroupSale">
        SELECT <include refid="baseSql"></include> FROM group_sale
        <where>
            <if test="flag != null">
                group_sale.flag = #{flag}
            </if>
            <if test="startTime != null">
                and group_sale.start_time > #{startTime}
            </if>
            <if test="endTime != null">
                and #{endTime} > group_sale.end_time
            </if>
        </where>
    </select>
    <delete id="deleteByGroupSaleIds">
        delete  from group_sale where id IN
        <foreach collection="groupSaleIds" open="(" separator="," close=")" item="groupSaleId" index="i">
            #{groupSaleId}
        </foreach>
    </delete>

    <!--<cache type="com.chemguan.business.core.util.RedisCache"/>-->
</mapper>