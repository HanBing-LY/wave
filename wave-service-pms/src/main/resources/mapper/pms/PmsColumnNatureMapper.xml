<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.liyuan.wave.pms.mapper.PmsColumnNatureMapper">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.liyuan.wave.po.pms.PmsColumnNature" id="pmsColumnNatureMap">
        <result property="id" column="id"/>
        <result property="natureName" column="nature_name"/>
        <result property="productColumnId" column="product_column_id"/>
        <result property="del" column="del"/>
        <result property="createName" column="create_name"/>
        <result property="modifyName" column="modify_name"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="queryByColumnId" resultType="com.liyuan.wave.pms.po.dto.PmsColumnNatureDto">
        SELECT
        id,
        nature_name,
        select_type,
        product_column_id
        FROM
        pms_column_nature
        WHERE
        id > ( SELECT id  FROM pms_column_nature  WHERE product_column_id = #{id} LIMIT #{start} ,1)
        LIMIT #{end}
    </select>


</mapper>