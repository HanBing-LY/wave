<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.liyuan.wave.usercenter.mapper.MenuMapper">
    <select id="selectPermissionByUserId" parameterType="string" resultType="com.liyuan.wave.po.ucenter.Menu">
        select m.* from
        user_role ur
        LEFT JOIN permission p
        on ur.role_id =p.role_id
        left join langchao_menu m
        on m.id = p.menu_id
        where user_id =#{id}
    </select>
</mapper>